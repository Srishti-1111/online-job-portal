

## India-wide demo database
`python manage.py seed_jobs` loads 650 India-wide academic/demo listings across states, cities, categories, job types and experience levels. These are not verified live vacancies.


### Dropdown fix
State and category dropdowns use distinct, alphabetically ordered values, so the same state/category does not repeat.
